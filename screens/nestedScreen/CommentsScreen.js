import {
    View,
    Text,
    StyleSheet,
    FlatList,
    Image,
    SafeAreaView,
    // TouchableOpacity
} from "react-native";


export default CommentsScreen = () => {
    return (
        <View>
            <Text>CommentsScreen</Text> 
        </View>
    );
}

const s = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
    },
    
})